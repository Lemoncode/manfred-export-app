import { IBorderOptions } from 'docx';

export const styles = {
  lineStyles: {
    border: {
      bottom: {
        color: 'DEDCE4',
        space: 100,
        style: 'single',
        size: 20,
      } as IBorderOptions,
    },
  },
};
